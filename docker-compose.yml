version: '3.8'

services:
  mongodb:
    image: mongo:latest
    container_name: deskshark-db
    restart: always
    ports:
      - "27017:27017"
    volumes:
      - deskshark-db-data:/data/db

volumes:
  deskshark-db-data: